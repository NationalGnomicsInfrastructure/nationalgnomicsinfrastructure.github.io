---
import Layout from '../../layouts/Layout.astro';
---
<Layout title="Project Identification" showTimer={true}>

  <div class="page-header">
    <div class="container">
      <h1>Project Identification</h1>
      <p>Identify your project using the Project Coordinator's spirit animal.</p>
    </div>
  </div>

  <section>
    <div class="container">
      <div class="submit-form">

        <div class="alert alert-info mb-2">
          <strong>Project identification.</strong><br/>
          Enter the Project Coordinator's spirit animal you identified to identify your project.
        </div>

        <div id="form-area">
          <div class="form-group">
            <label for="spirit-animal">Spirit Animal</label>
            <input type="text" id="spirit-animal" placeholder="Enter the spirit animal" autocomplete="off" style="font-size:1.1rem;" />
          </div>
          <button class="btn btn-primary" id="submit-btn" style="width:100%;padding:0.75rem;">
            Identify Project
          </button>
        </div>

        <div id="error-msg" style="display:none;" class="alert alert-warning mt-2">
          That's not the Project Coordinator's spirit animal. Try again.
        </div>

        <div id="result-area" style="display:none;">
          <div class="alert alert-success">
            Project identified. Your delivery continues.
          </div>
          <div class="card mt-3" style="padding:1.25rem;">
            <p style="font-style:italic;font-size:1.1rem;line-height:1.6;">
              "Ancients stellar objects, we are used to count days.<br/>
              How many are we?"
            </p>
            <p class="text-muted mt-2" style="font-size:0.9rem;">
              Find the envelope with that number and open it to continue your delivery.
            </p>
            <a href="/platform/" class="btn btn-primary mt-2">Back to Platform</a>
          </div>
        </div>

      </div>
    </div>
  </section>
</Layout>

<script>
const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;
const formArea = document.getElementById('form-area') as HTMLDivElement;
const resultArea = document.getElementById('result-area') as HTMLDivElement;
const errorMsg = document.getElementById('error-msg') as HTMLDivElement;
const input = document.getElementById('spirit-animal') as HTMLInputElement;

const savedAnimal = localStorage.getItem('spirit_animal');
if (!savedAnimal) {
  window.location.href = '/platform/';
}

if (localStorage.getItem('identification_done')) {
  formArea.style.display = 'none';
  resultArea.style.display = 'block';
}

submitBtn.addEventListener('click', () => {
  const answer = input.value.trim().toLowerCase();
  if (answer !== savedAnimal) {
    errorMsg.style.display = 'block';
    return;
  }
  errorMsg.style.display = 'none';
  localStorage.setItem('identification_done', '1');
  formArea.style.display = 'none';
  resultArea.style.display = 'block';
});

input.addEventListener('keydown', (e) => {
  if (e.key === 'Enter') submitBtn.click();
});
</script>
